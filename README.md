# dos_detection_prevention
This is an **educational cybersecurity sandbox** that combines a visual web simulator with real Python command-line tools to show how network floods (SYN/UDP/HTTP) are monitored using rate and entropy analytics, and mitigated using Linux `iptables` firewalls.
